import { Component, ReactNode } from "react";
import './Home.css';

export default class Home extends Component {
    render(): ReactNode {
        return (
            <div>
                Home
            </div>
        )
    }
}